<h1>Analysis</h1>
<ul>
  <% @analyses.each do |analysis| %>
    <li>
      <%= analysis.name %> - <%= analysis.price %> ₴
      <%= button_to 'Add to order', order_order_items_path(order_id: @order_id, analysis_id: analysis.id), method: :post %>
    </li>
  <% end %>
</ul>
