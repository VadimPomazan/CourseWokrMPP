<p style="color: green"><%= notice %></p>
<div>
  <%= link_to t('actions.edit'), edit_profile_path %>
</div>

<%= render @profile %>


<% if current_user.role == "client" %>
  <div>
    <%= link_to t('family_member.view'), family_members_path(user_id: current_user.id) %>
  </div>
<% end %>
